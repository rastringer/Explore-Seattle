//categorize data for backend and return array of categories

categorizer = function(name, description){

  if (description == undefined || description == null){
    return recommend(name);
  }else{
    return recommend(name);
  }
};
